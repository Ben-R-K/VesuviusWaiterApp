// login_screen.dart removed — Login screen has been removed per user request.
// The application starts at the TableOverview screen and uses the AppTheme.

// If you need to re-add the login screen later, create a new file here and update
// `lib/main.dart` to route to the login screen before navigating to table overview.

